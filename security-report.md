# 🔒 安全检查报告

**时间**：2026-07-23 17:31
**扫描范围**：src/ + 配置文件（含全局 `~/.claude/settings.json`）

## 总览

| 类别 | 发现数 | 严重度 |
|------|--------|--------|
| 硬编码敏感信息 | 1 | 🔴 严重 |
| 注入漏洞 | 0 | ✅ 无 |
| 配置文件泄露 | 0 | ✅ 项目内无 |
| 其他隐患 | 0 | ✅ 无 |
| 依赖漏洞 | 未知 | 🟡 无法检测 |

## 详细发现

### 🔴 严重

| 文件:行号 | 类别 | 问题 | 建议修复 |
|-----------|------|------|----------|
| `~/.claude/settings.json:4` | 硬编码 API 密钥 | `ANTHROPIC_AUTH_TOKEN: "sk-c33baa2b73504c24bc7c350f299ec12f"` 明文写在配置文件中 | 将密钥移到系统环境变量，配置文件引用 `${ANTHROPIC_AUTH_TOKEN}` |

**详细说明**：你的 Claude Code 全局配置文件直接写入了 API 认证令牌（token）。如果这个文件被分享出去（比如上传到公开仓库、截图分享、或被他人访问），任何人拿到这个 token 就能以你的身份调用 API，产生费用或泄露数据。

### 🟡 注意事项

| 事项 | 说明 |
|------|------|
| npm audit 不可用 | 当前使用 npmmirror 镜像，不支持 `npm audit`。建议偶尔切换到官方源运行：`npm audit --registry https://registry.npmjs.org` |
| 项目内代码 | ✅ `src/` 下未发现硬编码密码、SQL 注入、XSS 等风险（本项目是纯前端记账应用，数据存在 localStorage，不涉及网络传输） |
| 环境变量文件 | ✅ 项目无 `.env` 文件，敏感信息未泄露在项目目录中 |

## 安全评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 源码安全 | ⭐⭐⭐⭐⭐ | 代码无安全漏洞 |
| 配置安全 | ⭐⭐⭐ | 全局配置中有明文密钥 |
| 依赖安全 | ❓ | 无法检测 |

## 建议

1. **立即处理**：将 `ANTHROPIC_AUTH_TOKEN` 从 `settings.json` 中移除，改用系统环境变量
2. **定期检查**：每次提交前运行 `/security-check` 确保没有新的敏感信息被写入代码
