# 注释检查报告

**时间**：2026-07-26 21:16
**扫描范围**：src/

## 总览

| 指标 | 数值 |
|------|------|
| 扫描文件数 | 13 |
| 检查函数/逻辑块数 | 45 |
| 缺失注释 🔴 | 1 |
| 注释不足 🟡 | 3 |
| 合格 🟢 | 41 |

## 缺失注释 🔴

### SnakeGame.tsx
| 行号 | 函数/代码 | 问题 |
|------|----------|------|
| 79 | `SnakeGame()` 组件 | 无任何注释。作为包含约 430 行的复杂游戏组件，缺少顶层的 JSDoc 说明组件功能、状态管理和游戏机制。 |

## 注释不足 🟡

### SnakeGame.tsx
| 行号 | 函数/代码 | 现有注释 | 问题 |
|------|----------|---------|------|
| 97 | `draw()` | `// ========== 绘制函数 ==========` 分组标题 | 函数本身约 135 行，包含画布初始化、网格线绘制、食物渲染、蛇身渲染（含蛇头圆角+眼睛）、暂停遮罩等多个子任务。分组标题过于笼统，建议添加函数级 JSDoc 说明绘制流程。 |
| 235 | `tick()` | `// ========== 游戏主循环 ==========` 分组标题 | 函数约 55 行，包含方向应用、碰撞检测（撞墙/撞自己）、蛇身移动、食物生成、加速逻辑。分组标题不足以描述游戏主循环的具体步骤，建议添加函数级 JSDoc。 |
| 305 | `drawGameOver()` | 与 `endGame()` 共用 `// ========== 游戏结束 ==========` 标题 | 独立函数但无单独注释。建议补充说明其"延迟 50ms 绘制半透明遮罩和结束文字"的行为。 |

## 合格示例 🟢

- **storage.ts**：所有导出函数均有清晰的 JSDoc，解释了功能、参数和返回值。工具函数 `read()`/`write()` 的泛型说明也很到位。
- **AppContext.tsx**：`AppProvider` 有详细的多行 JSDoc，所有操作函数和计算函数均有注释，`getMonthTrend` 还标注了 `@params` 和 `@returns`。
- **所有页面组件和通用组件**：`Home`, `Records`, `Statistics`, `Settings`, `AddRecordModal`, `BudgetBar`, `Layout`, `MonthPicker` 均有函数级 JSDoc。
- **SnakeGame.tsx**：关键逻辑有良好的内联注释（如"撞墙检测"、"撞自己检测（排除尾巴）"、"吃到食物：不缩尾巴"、"加速"等），代码分节清晰。
